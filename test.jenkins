pipeline {
		agent any
		stages{
		    
			stage('Git Check-out'){
				steps{
				git 'https://github.com/thockin/test.git'
				sh"git rev-parse --short HEAD"
				}
			}
				stage('Commmit Code'){
					steps{
					sh'''
					com=echo ${env.GIT_COMMIT}
					sha=echo ${com:0:6 }
					sha1=echo ${env.GIT_COMMIT:0:6 }
					echo ${sha1}
					export sha
					'''
					sh"echo ${env.GIT_COMMIT}"
					sh"echo ${sha}"
					
					sh"echo ${env.BRANCH_NAME}"
					}
				}
			}
	}
